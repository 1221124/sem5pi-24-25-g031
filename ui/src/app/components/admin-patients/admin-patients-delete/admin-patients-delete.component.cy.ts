import { AdminPatientsDeleteComponent } from './admin-patients-delete.component'

describe('AdminPatientsDeleteComponent', () => {
  it('should mount', () => {
    cy.mount(AdminPatientsDeleteComponent)
  })
})
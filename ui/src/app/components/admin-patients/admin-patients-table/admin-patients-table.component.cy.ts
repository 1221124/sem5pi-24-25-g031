import { AdminPatientsTableComponent } from './admin-patients-table.component'

describe('AdminPatientsTableComponent', () => {
  it('should mount', () => {
    cy.mount(AdminPatientsTableComponent)
  })
})
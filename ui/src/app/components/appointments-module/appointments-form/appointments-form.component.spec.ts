import { AppointmentsFormComponent } from './appointments-form.component'

describe('AppointmentsFormComponent', () => {
  it('should mount', () => {
    expect(AppointmentsFormComponent).toBeTruthy()
  })
})
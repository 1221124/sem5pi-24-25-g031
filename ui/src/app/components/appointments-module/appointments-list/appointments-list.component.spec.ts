import { AppointmentsListComponent } from './appointments-list.component'

describe('AppointmentsListComponent', () => {
  it('should mount', () => {
    expect(AppointmentsListComponent).toBeTruthy()
  })
})
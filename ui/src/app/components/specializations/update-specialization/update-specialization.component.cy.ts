import { UpdateSpecializationComponent } from './update-specialization.component'

describe('UpdateSpecializationComponent', () => {
  it('should mount', () => {
    cy.mount(UpdateSpecializationComponent)
  })
})
import { SlotAppointmentStaffsComponent } from './slot-appointment-staffs.component'

describe('SlotAppointmentStaffsComponent', () => {
  it('should mount', () => {
    cy.mount(SlotAppointmentStaffsComponent)
  })
})
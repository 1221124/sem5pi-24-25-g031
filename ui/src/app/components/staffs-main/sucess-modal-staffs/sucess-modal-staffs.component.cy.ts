import { SucessModalStaffsComponent } from './sucess-modal-staffs.component'

describe('SucessModalStaffsComponent', () => {
  it('should mount', () => {
    cy.mount(SucessModalStaffsComponent)
  })
})